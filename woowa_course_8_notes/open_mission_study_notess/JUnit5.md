# 📖 지식 정리 (우테코를 하며 학습한 내용)
## 1️⃣ @Nested
### ✔️ @Nested
`@Nested`는 `JUnit5에서 도입된 기능으로, 
어노테이션이 있는 클래스를 둘러싸고 있는 클래스의 인스턴스와 설정 및 상태를 공유할 수 있는 중첩된 비정적(non-static) 테스트 클래스이다. ex) inner class

둘러쌓는 클래스는 최상위 클래스일 수도 있고, `@Nested` 테스트 클래스일 수도 있으며, 중첩은 깊어질 수 있다.

`@TestClassOrder` 또는 전역 `ClassOrder`를 이용해 순서를 지정할 수 있다.

`@Nested` 테스트 클래스는 둘러쌓는 테스트 클래스와 다룰 수 있는 자체 `TestInstance.Lifecycle` 모드를 구성할 수 있다.

`@BeforeAll`, `@AfterAll`이 사용이 불가하며, 해당 어노테이션은 상속이 되지 않는다.

`@Nested` 테스트는 여러 테스트 그룹 간의 관계를 표현할 수 있고, 계층적으로 나타낼 수 있다.

> 💡 **정보**
> - 정적 클래스가 아닌 Inner Class에만 `@Nested`를 사용할 수 있다.
> - 내부 클래스는 `@BeforeAll`과 `@AfterAll`을 제외하고 라이프 사이클이 지원된다.
>   - 그 이유는 Java에서 inner class에 static을 허용하지 않기 때문이다.
>   - 그러나 이와 같은 문제는 `@TestInstance(Lifecycle.PER_CLASS)`를 이용해 우회할 수 있다.

> 📌 **정리**
> - `@Nested` 어노테이션을 이용해 중첩 클래스를 이용해 계층적으로 테스트를 작성할 수 있다.
> - 계층적으로 테스트를 작성하게 되면 테스트 그룹화가 쉬워지고, 테스트를 이해하는데 편리해진다.

```java
class CarValidatorTest {
    @Nested
    @DisplayName("자동차 이름 형식 검증")
    class  NameValidationTest {
        // ...
    }
    
    @Nested
    @DisplayName("자동차 개수 검증")
    class  CarCountValidationTest {
        // ...
    }
    
}
```

---

### ✔️ @TestInstance(Lifecycle.XXX)는 무엇인가?
`@TestInstance` 어노테이션은 테스트 클래스 또는 테스트 인터페이스에 대한 테스트 인스턴스의 수명 주기를 구성하는 사용한다.

`default` 값으로는 `PER_METHOD`로 설정이 되며, 모든 중첩 테스트 클래스 내에 상속이 된다.

테스트 인스턴스의 `Lifecycle Mode`를 `PER_CLASS`로 설정하면 아래와 같은 기능이 활성화 된다. 
- 테스트 클래스의 `@BeforeAll`, `@AfterAll` 메서드 뿐만 아니라 테스트 클래스의 테스트 메서드 간에 인스턴스 상태를 공유한다.
- `@Nested` 클래스에서 `@BeforeAll`, `@AfterAll`을 사용 가능하게 된다.

> - `PER_CLASS`: 이 모드를 사용할 때 새 테스트 인스턴스는 테스트 클래스당 한 번 생성된다.
> - `PER_METHOD`: 이 모드를 사용하면 테스트 메서드에 대해 새 테스트 인스턴스가 생성된다.

---

## 2️⃣ 테스트 코드 메서드명
- 7가지 정도 있는데, `given_사전조건_when_테스트상태_expect_기대결과` 방식으로 결정

> **위 방식의 특징**
> - 테스트를 세개의 파트로 나누어 전제 조건, 테스트 상태, 기대 결과가 위 형식대로 네이밍되는 형태이다.
>
> **장점**: 테스트를 해볼 조건이 뭐고 어떻게하면 뭐가나오는지 전부 적혀있다.
>
> **단점**: 메서드 명이 지나치게 길어지는 경향이 있다.

---

## 3️⃣ assetThatCode, assertThrownThrownBy
`assetThatCode`: 에러가 발생하면 안 된다. 에러가 발생하면 실패

`assertThrownThrownBy`: 무조건 에러가 발생해야 한다. 에러가 발생하지 않으면 실패

---

## 🔖 References
- [JUnit5의 @Nested를 이용해 테스트 작성해보자](https://rutgo-letsgo.tistory.com/entry/Junit5%EC%9D%98-Nested%EB%A5%BC-%EC%9D%B4%EC%9A%A9%ED%95%B4-%ED%85%8C%EC%8A%A4%ED%8A%B8-%EC%9E%91%EC%84%B1%ED%95%B4%EB%B3%B4%EC%9E%90)
- [테스트 코드 메서드 이름에 관하여..](https://velog.io/@no1msh1217/%ED%85%8C%EC%8A%A4%ED%8A%B8-%EC%BD%94%EB%93%9C-%EB%A9%94%EC%84%9C%EB%93%9C-%EC%9D%B4%EB%A6%84%EC%97%90-%EA%B4%80%ED%95%98%EC%97%AC)
