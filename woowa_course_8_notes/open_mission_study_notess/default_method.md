# 📖 지식 정리 (우테코를 하며 학습한 내용)
## 1️⃣ default method란?
보통 인터페이스 규칙을 잡거나, 서비스의 플로우 로직을 잡는데 사용하곤 한다.

하지만 인터페이스를 구현하는 클래스에서는 메서드를 모두 구현해야 하기 때문에 인터페이스에 메서드를 추가할 때 문제가 발생한다.
메서드 하나를 추가하려면 해당 인터페이스를 구현하는 모든 클래스에서는 해당 메서드를 모두 구현해줘야 하는 것이다.

Java 8에서는 이러한 문제를 해결하기 위해 2가지 방법을 제공한다.

1. 인터페이스 내부에 정적 메서드(static method)를 사용한다.
2. 인터페이스의 기본 구현을 제공할 수 있도록 default method 기능을 사용한다.

인터페이스에 디폴트 메서드를 사용하여 메서드를 구현할 수 있다.

**디폴트 메서드를 이용하면 인터페이스의 기본 구현을 그대로 상속하므로 인터페이스 자유롭게 메서드를 추가할 수 있게 된다.**
이로 인해 호환성을 유지하면서 API를 바꿀 수 있다.

---

## ☑️ 고민한 내용
입출력 메시지나 에러 메시지처럼 공통적으로 사용하는 `format()`과 `prependNewLineToMessage()` 두 메서드를 어디에 둘지 고민이 많았다.

처음에는 별도의 Util 클래스를 만들어 정적 메서드 형태로 두려 했지만, 호출 방식이 객체지향적이지 않고 사용성이 떨어졌다.

그러던 중 인터페이스의 `default 메서드`를 활용하면 이러한 공통 로직을 훨씬 자연스럽게 녹여낼 수 있다는 점을 알게 되었고, 실제로 적용해 보았다.

인터페이스에 두 메서드를 `default`로 선언하고, 각 메시지 `enum`이 해당 인터페이스를 구현하도록 구성하자
`MessageUtils.format(...)`처럼 번거로운 정적 호출 없이
`OutputMessage.PURCHASE.format()` 처럼 메시지 객체가 스스로 행위를 수행하는 형태가 되었다.

이 방식은 “**메시지가 스스로 자신의 출력 형식을 책임진다**”는 설계 관점에서도 훨씬 더 객체지향적이며, 코드의 응집도가 높아지고 확장성도 좋아졌음을 직접 체감할 수 있었다.

default 메서드가 단순한 문법 요소가 아니라, 인터페이스에 공통 로직을 부여하면서도 구현체의 부담을 줄일 수 있는 강력한 도구라는 점을 다시 한 번 느끼게 되었다.

---

## 🔖 References
- [[모던 자바] 디폴트 메서드(default method)란?](https://devbksheen.tistory.com/entry/%EB%94%94%ED%8F%B4%ED%8A%B8-%EB%A9%94%EC%84%9C%EB%93%9Cdefault-method%EB%9E%80)
