# 📖 지식 정리 (우테코를 하며 학습한 내용)
## 1️⃣ 테스트 코드 학습

### 단위 테스트(Unit Test)란
> 메서드를 테스트하는 또 다른 메서드이다. 하나의 단위 테스트는 하나의 메서드의 특정 루틴을 검사한다.

---
### 단위 테스트가 왜 필요한가
```text
단위 테스트는 단순히 버그를 찾기 위한 효과적인 방법이 아니다.
정의에 따르면 단위 테스트는 시스템의 각각의 단위들을 개별적으로 조사하는 것이다.

잘 구성된 테스트 코드는 이후 다른 변경 사항으로 인해 발생 가능한 결함을 찾아내는 역할을 한다.
```

---
### 하나의 테스트 케이스는 단위 기능 중 하나의 시나리오만 테스트하라
```text
테스트 코드에서 두 가지 이상의 경우(시나리오)를 테스트한 경우, 어떤 문제가 발생할 수 있을까?

두 가지 이상의 시나리오 중 하나가 에러가 발생한다면,
해당 테스트 코드에 빨간 불이 들어오겠지만, 
우리는 어떤 시나리오에서 문제가 발생했는지 파악하기가 쉽지 않다.

따라서 시나리오를 각각의 테스트 메서드로 분리해서 작성해야지 어떤 시나리오에서 에러가 발생했는지 명확하게 파악할 수 있따.
```

---
### 단위 테스트하기 어려운 코드를 단위 테스트하기 좋은 코드로 만들기
```text
🤔 테스트 코드의 중요성
1. 제품의 안정성을 높여준다.
2. 기능의 추가 및 수정으로 인한 부작용(Side-Effect)를 줄일 수 있다.
3. 불안감 없이 코드 작성을 할 수 있도록 도와준다.
4. 디버깅을 쉽게 해준다.
5. 개발 과정에서 반복적인 작업들을 하지 않도록 도와준다.
6. 더 깔끔하고 재사용성이 좋은 코드 작성을 가능하게 해준다.
```

> 🤔 **테스트하기 어려운 메서드**
> ```java
> public void move() {
>    final int number = random.nextInt(RANDOM_NUMBER_UPPER_BOUND);
>
>    if (number >= MOVABLE_LOWER_BOUND) {
>        position++;
>    }
> }
> ```
> `move`에 대한 테스트 코드를 어떻게 작성할 것인지?
> - 위의 시나리오를 테스트하기 위해서는 `move()` 메서드 내의 `number`를 우리가 원하는 대로 설정할 수 있어야 한다.
>   - 하지만 `number`에 저장되는 랜덤 값은 `move` 메서드 내부에서 생성되기 때문에 우리가 의도하는 대로 설정할 수 없다.
> - 그렇다면 `move()` 메서드를 어떻게 수정해야 테스트하기 좋은 메서드로 바꿀 수 있을까?
> 
> 메서드 시그니처를 수정하여 테스트하기 좋은 메서드로 만들기
> - 먼저 `number` 변수를 밖으로 빼야 한다. 그렇다면 `number`에 의도하는 값을 넣을 수 있고,
>   - move() 메서드가 `number` 값에 따라 의도하는 대로 실행하는지 확인할 수 있다.

> 📌 정리
> - 우리는 메서드 시그니처를 수정하는 것만으로 테스트하기 좋은 메서드로 만들 수 있다는 것을 알게 되었다.
> - 물론 이렇게 수정했다고 모든 문제가 해결된 것이 아님. 단지 랜덤한 값에 의존하는 클래스의 위치가 변경된 것이기 때문이다.
>   - 실제로 `move()` 메서드를 사용하는 다른 클래스에서 랜덤 한 값을 주입해 주어야 하기 때문에, 해당 클래스에서는 다시 랜덤한 값에 의존하게 된다.
> - 우리는 이러한 의존을 어디에 두고, 어떻게 관리해야 할 것인지 고민하여 테스트하기 좋은 구조로 코드를 작성할 수 있을 것이다.
> - 의존을 조금 더 줄일 수 있는 방법 중 하나로, 인터페이스를 분리하여 테스트 하기 좋은 메서드로 만드는 방법을 알아보자!

---
### 인터페이스를 분리하여 테스트하기 좋은 메서드로 만들기
메서드 시그니처를 수정하는 방법은 의존 관계를 상위로 이동시킨 것에 불과하다.

기존 코드의 응집도는 그대로 유지하되, 테스트하기 좋은 형태로 수정하는 좋은 방법이 없을까?

프로덕션 코드가 동작하는 곳에서는 `move()` 메서드가 랜덤 한 값을 받로록 하고,
테스트 코드가 동작하는 곳에서는 `move()` 메서드가 의도한 값을 받도록 하면 된다.

위에서 말한 방법이 가능하도록 하는 것이 바로 **인터페이스 분리**이다.

> 🤔 전략 패턴을 사용한 인터페이스 분리
> - 인터페이스인 `NumberGenerator` 간단하게 구현
> - `move()` 메서드에 매개변수를 `NumberGenerator`로 받는다.
> - 이제는 `move()` 메서드에서 숫자를 생성하는 역할을 담당하는 `NumberGenerator` 인터페이스를 랜덤 한 값 또는 의도하는 값을 생성할 수 있도록 구현체를 정의하면 된다.

---

## ☑️ 고민한 내용
> ‼️ 느낌 1
> - 테스트 코드를 작성하면서 **private 메서드를 어떻게 테스트해야 하는지**에 대한 고민이 생겼다.
> - "3주차 공통 피드백에서 “private 함수를 테스트하고 싶다면 클래스를 분리하여 테스트 가능성을 높여라” 라는 내용을 떠올렸고,
> 실제로 `ParseUtils` 내부에 있는 기본 검증 로직(입력값 blank, 숫자 범위 등)을 private 형태로 두니 테스트가 매우 어려웠다.
> - 결국 **검증 로직을 별도 클래스로 분리하는 것이 더 적절하다고** 판단했고, 이를 통해 테스트 가능성과 응집도를 동시에 개선해보려고 한다.


> ‼️ 느낌 2
> - 테스트 코드를 작성하다 보니 **하드코딩을 어떻게 줄일지**에 대한 고민도 함께 생겼다.
> - 공통 피드백에서 “하드코딩을 피하자” 라고 했지만, 처음부터 모든 테스트 데이터를 매개변수화하면 오히려 속도가 너무 느려졌다.
> - 그래서 **초기에는 하드코딩으로 빠르게 테스트 흐름을 잡고**,
> 이후에 `@ParameterizedTest` 또는 `Fixtures`로 전환하여 **중복 제거 + 가독성 개선**을 하는 방식이 더 현실적이라는 결론에 도달했다.


> 🤔 배운점 1
> - 입력값 검증을 위해 `input == null || input.isBlank()`를 사용하다가 코드 리뷰에서 `StringUtils.isBlank()`를 추천받아 사용했는데,
> 나중에 보니 **StringUtils가 JUnit 유틸 클래스여서 메인 로직에서 사용하는 것은 부적절**하다는 사실을 알게 되었다.
> 
> (런타임 의존성이 꼬이기 때문!)
> - 앞으로는 기존 방식대로 직접 null/blank 체크를 구현해야겠고, 이 내용을 알려준 분에게도 공유해야겠다.


> 🤔 배운점 2
> - 테스트는 성공/실패 기준으로만 구획을 나누는 것이 아니라,
> **검증하고자 하는 메서드 단위로 묶어야 유지보수가 훨씬 좋다**는 것을 깨달았다.
> 
> (특히 validation 테스트에서 효과적!)


> 🤔 배운점 3
> - VO 객체 테스트 시, 입력 리스트 변경 여부를 확인하며
> **내부에서 방어적 복사가 잘 구현되었는지** 확인하는 과정이 매우 중요하다는 것을 배웠다.
>   - 원본 리스트를 바꿔도 내부 값이 안 변하는지
>   - 반환 리스트에 수정 작업을 시도하면 `UnsupportedOperationException`이 발생하는지
> - 즉, **외부에서 어떤 조작을 해도 VO 내부 상태는 절대 변하지 않아야 한다**는 것을 명확히 검증해야 한다는 점을 다시 한번 체감했다.

---

## 🔖 References
- [Tecoble 2기_스티치](https://tecoble.techcourse.co.kr/author/2-%EA%B8%B0-%EC%8A%A4%ED%8B%B0%EC%B9%98/)

---
