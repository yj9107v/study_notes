# 📖 지식 정리 (우테코를 하며 학습한 내용)
## 1️⃣ 함수형 인터페이스란?
- [함수형 인터페이스 정리](https://github.com/yj9107v/study_notes/blob/main/java_study_notes/Inflearn_Lecture/JAVA_ADV3/Chapter3_%ED%95%A8%EC%88%98%ED%98%95_%EC%9D%B8%ED%84%B0%ED%8E%98%EC%9D%B4%EC%8A%A4.md)

---

## ☑️ 고민한 내용
- 함수형 인터페이스는 자바를 공부했을 때 잠깐 공부했었다.
- 이론은 알고 있었지만 실전으로 적용하려고 하니 막상 구현할 방법을 잘 몰랐다.

**`SupplierWithValidation` 커스텀 함수형 인터페이스 제거**
- 지난 과제에서는 입력 단계마다 반복되는 `while - try - catch` 구조를 줄이기 위해,
  코드 조각을 전달받는 `SupplierWithValidation`이라는 **직접 만든 함수형 인터페이스**를 사용했다.

- 그러나 공부를 이어가면서, 자바에는 이미 **Supplier, Function, Consumer** 등 표준 함수형 인터페이스가 충분히 제공되고 있으며,
  **특별한 이유가 없다면 커스텀 함수형 인터페이스를 만들지 않는 것이 더 바람직하다**는 점을 알게 되었다.

- 내가 필요한 동작은 단순했다:
    - "**코드를 실행해보고, 오류가 발생하면 예외를 던져 다시 시도한다**"
    - → 이 구조는 굳이 직접 인터페이스를 만들 필요 없이 자바의 `Supplier<T>`로도 충분히 구현할 수 있었다.

- 그래서 새로운 입력 재시도 로직(`RetryExecutor`)은 **표준 `Supplier<T>`를 활용하는 방식**으로 변경했다.
- 표준 함수형 인터페이스를 사용함으로써 얻은 장점은 다음과 같다:
    - **중복된 인터페이스 정의를 제거**해 코드가 더 단순해짐
    - 다른 개발자들이 코드를 이해하기 쉬워 **재사용성과 유지보수성이 향상**
    - 자바가 기본적으로 제공하는 인터페이스 체계와 일관성을 유지하여 **개발 관례에 부합하는 구조**가 됨
